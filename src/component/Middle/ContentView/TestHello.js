import React from 'react';

const TestHello = ({posts}) => {
    console.log(posts.post[0]);

    // const post=posts.map((p)=>{

    // })
    return (
        <div>
            
        </div>
    );
};

export default TestHello;